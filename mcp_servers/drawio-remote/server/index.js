// Placeholder entry point. This bundle does not run a local MCP server;
// it spawns `npx mcp-remote <url>` (see manifest.json -> server.mcp_config)
// to bridge Claude Desktop's stdio transport to the remote HTTP MCP endpoint.
